<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <!-- Permissions for Internet and Network -->
    <uses-permission android:name="android.permission.INTERNET"/>
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE"/>
    <uses-permission android:name="android.permission.READ_EXTERNAL_STORAGE"/>
    <uses-permission android:name="android.permission.WRITE_EXTERNAL_STORAGE"/>

    <application
        android:allowBackup="true"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.MemoryConnect"
        tools:targetApi="31">

        <!-- Splash Activity -->
        <activity
            android:name=".splash.SplashActivity"
            android:theme="@style/SplashScreenTheme"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <!-- Login Activity -->
        <activity android:name=".controllers.LoginActivity" />

        <!-- Sign Up Activity -->
        <activity android:name=".controllers.SignUpActivity" />

        <!-- Profile Activity -->
        <activity android:name=".controllers.ProfileActivity" />

        <!-- Upload Activity -->
        <activity android:name=".controllers.UploadActivity" />

        <!-- Add to Timeline Activity -->
        <activity
            android:name=".controllers.AddToTimelineActivity"
            android:label="@string/add_to_timeline_label"
            android:theme="@style/Theme.MemoryConnect" />

        <!-- Patient Timeline Fragment -->
        <activity android:name=".controllers.patient_timeline_fragment" />



        <!-- Other Activities -->


        <!-- Firebase Configuration -->
        <meta-data
            android:name="com.google.firebase.messaging.default_notification_channel_id"
            android:value="default" />

    </application>

</manifest>
